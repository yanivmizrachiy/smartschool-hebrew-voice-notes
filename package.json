{
  "name": "harut-workbook",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "build:print": "node src/build-print.mjs",
    "validate": "node tests/validate.mjs",
    "check": "npm run build && npm run build:print && npm run validate"
  }
}
