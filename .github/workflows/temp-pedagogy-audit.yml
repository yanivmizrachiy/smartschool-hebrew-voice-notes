name: Temporary pedagogy audit
on:
  push:
    branches:
      - fix/pedagogy-visual-print-polish-20260813
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Report every worksheet question
        run: node tests/pedagogy-audit-report.mjs
